spring.application.name=api5012
spring.config.import=optional:classpath:application-secret.properties,optional:file:./src/main/resources/application-secret.properties,optional:file:application-secret.properties

# ============================================================
# [부산도서관 관리 시스템] MariaDB 설정 안내
# ============================================================
# MariaDB 를 사용하려면 아래 주석을 해제(#제거)하고
# PostgreSQL 설정을 주석 처리하세요.
#
# 사전 준비:
#   1. MariaDB 설치 및 실행
#   2. DB 생성: CREATE DATABASE library_db CHARACTER SET utf8mb4;
#   3. 사용자 권한: GRANT ALL ON library_db.* TO 'root'@'localhost';
#
# pom.xml 에 MariaDB 드라이버 의존성도 추가해야 합니다:
#   <dependency>
#       <groupId>org.mariadb.jdbc</groupId>
#       <artifactId>mariadb-java-client</artifactId>
#       <scope>runtime</scope>
#   </dependency>
# ============================================================

# MariaDB Configuration for Library System (부산도서관 관리 시스템)
#spring.datasource.url=jdbc:mariadb://localhost:3306/library_db
#spring.datasource.username=root
#spring.datasource.password=1234
#spring.datasource.driver-class-name=org.mariadb.jdbc.Driver

# Hibernate settings for MariaDB
spring.jpa.database-platform=org.hibernate.dialect.MariaDBDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.show-sql=true

# ============================================================
# 기존 MariaDB Configuration (webdb 계정용)
# ============================================================
spring.datasource.url=jdbc:mariadb://localhost:3306/webdb
spring.datasource.username=webuser
spring.datasource.password=webuser
spring.datasource.driver-class-name=org.mariadb.jdbc.Driver


#spring.datasource.url=jdbc:postgresql://localhost:5432/mydb
#spring.datasource.username=webdb
#spring.datasource.password=webdb
#spring.datasource.driver-class-name=org.postgresql.Driver

#spring.jpa.hibernate.ddl-auto=update
#spring.jpa.show-sql=true
#spring.jpa.properties.hibernate.format_sql=true
#spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect




#logging
logging.level.org.springframework=info
logging.level.com.busanit501=debug
logging.level.org.springframework.security=debug

# ?? ??? ?? ?? ?? (?: 500MB)
server.tomcat.connection-timeout=30s
spring.servlet.multipart.max-file-size=500MB
spring.servlet.multipart.max-request-size=500MB

com.busanit5012.jwt.secret=busainit501234567890busainit501234567890busainit501234567890

# Gemini AI defaults
gemini.api.base-url=https://generativelanguage.googleapis.com/v1beta/models
gemini.api.model=gemini-2.5-flash
